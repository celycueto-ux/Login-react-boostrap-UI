import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Login from './componentes/Login'

const divRoot = document.querySelector('#root');

ReactDOM.render(<Login />, divRoot);